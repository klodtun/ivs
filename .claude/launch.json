{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend-api",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "cd backend && source venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"],
      "port": 8000
    },
    {
      "name": "frontend-dashboard",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "cd frontend && BACKEND_URL=http://localhost:8000 NODE_OPTIONS=--no-deprecation npx next dev -p 3000"],
      "port": 3000
    }
  ]
}
